def count_items(shopping_list: list[str]):
    # shopping_list=[]
    if len(shopping_list) == 0:
        return 0
    else:
       
        return len(shopping_list)
    
print(count_items(["apple","banana","juice"]))
